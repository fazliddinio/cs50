# functions practice
from functools import reduce

def greet(name):
    return f"Hello, {name}!"

def greet_with_title(name, title="Mr."):
    return f"Hello, {title} {name}!"

# args kwargs

# lambda map filter
